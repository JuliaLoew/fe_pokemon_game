import React, { useEffect, useState } from "react";
import { useParams, useNavigate } from "react-router-dom";


const Leaderboard = () => {
    return <div className='text-2xl'>"Leaderboard Page:
    Display a leaderboard with the top players based on their scores.
    Allow the user to enter their name and persist their score in the database after a battle." </div>;
  };
  
  export default Leaderboard;
  