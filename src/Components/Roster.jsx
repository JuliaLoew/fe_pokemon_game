import React, { useEffect, useState } from "react";
import { useParams, useNavigate } from "react-router-dom";


const Roster = () => {
    return <div className='text-2xl'>"My Roster Page:
    Display the list of Pokemon the user has selected.
    Include an option to remove Pokemon from the roster.
    You can either persist this to localStorage or to the database." </div>;
  };
  
  export default Roster;
  