import React, { useEffect, useState } from "react";
import { useParams, useNavigate } from "react-router-dom";


const Battle = () => {
    return <div className='text-2xl'>"Battle Page:
    Allow the user to engage in battles with random Pokemon.
    Use Pokemon stats and types to create a simple battle dynamic (suggestion: type advantages/disadvantages, higher stat wins the round).
    Track the user’s wins and losses, assigning points or experience (XP) for each win." </div>;
  };
  
  export default Battle;
  